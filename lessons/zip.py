"""Combining two lists into a dictionary."""
__author__ = "730668363"

def zip(l1: list[str], l2: list[int]) -> dict[str, int]:
    if len(l1) != len(l2):
        zip == ()
    else:
        zip: dict[str, int] = {l1: l2}
        for key in zip:
            return (zip)

print(zip(["Happy", "Tuesday"], [1, 2]))
    
